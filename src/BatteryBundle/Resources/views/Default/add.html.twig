{% extends '::base.html.twig' %}

{% block body %}
    <h1>Add new batteries</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Add battery" class="btn btn-default">
    {{ form_end(form) }}
{% endblock %}
